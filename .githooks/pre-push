#!/bin/sh
# Auto-deploy Service Remote su Firebase quando si fa push su main.
ROOT="$(git rev-parse --show-toplevel)"
powershell.exe -NoProfile -ExecutionPolicy Bypass -File "$ROOT/_auto-deploy-remote-on-push.ps1"
exit 0
