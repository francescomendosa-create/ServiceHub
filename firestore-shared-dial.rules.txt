// Firestore — stato condiviso tra TUTTI i dispositivi (stessi numeri, stesso Digital quando pubblicato).
// Incolla questo blocco dentro le regole del progetto (Firebase Console → Firestore → Regole),
// dentro match /databases/{database}/documents { ... }

    match /artifacts/{appId}/sharedDial/{document=**} {
      // Qualsiasi utente autenticato (anche anonimo Firebase) legge/scrive lo stesso albero:
      // sharedDial/plant, settings, alarmConfig, aiMemory, aiChat, meta, plant/snapshots/...
      allow read, write: if request.auth != null;
    }
